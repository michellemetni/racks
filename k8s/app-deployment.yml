apiVersion: apps/v1
kind: Deployment
metadata:
  name: racks-app
  namespace: racks
spec:
  replicas: 1
  selector:
    matchLabels:
      app: racks-app
  template:
    metadata:
      labels:
        app: racks-app
    spec:
      containers:
        - name: racks-app
          image: ghcr.io/michellemetni/racks:latest
          ports:
            - containerPort: 8080

          envFrom:
            - configMapRef:
                name: racks-config
            - secretRef:
                name: racks-secret

          readinessProbe:
            tcpSocket:
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10

          livenessProbe:
            tcpSocket:
              port: 8080
            initialDelaySeconds: 90
            periodSeconds: 20
